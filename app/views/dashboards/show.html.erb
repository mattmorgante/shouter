<%= form_for @dashboard.new_text_shout do |form| %> 
  <%= form.text_field :body, placeholder: 'This is a placeholder' %>
  <%= form.submit 'Submit' %>
<% end %>

<%= form_for @dashboard.new_photo_shout do |form| %> 
  <%= form.file_field :image %>
  <%= form.submit 'Submit' %>
<% end %>


<%= render @dashboard.timeline %>